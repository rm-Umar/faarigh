package com.faarigh.app.widget

// This file is no longer needed — the widget subclasses (FaarighWidgetSmall,
// FaarighWidgetMedium, FaarighWidgetLarge) are registered directly as receivers
// in the manifest. Kept as an empty file to avoid stale references.
